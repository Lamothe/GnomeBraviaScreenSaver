# Bravia Quick Toggle

This GNOME Shell extension adds a **Bravia TV** toggle to Quick Settings. It
uses the installed `gnome-bravia-screensaver` command, so the TV address and
Simple IP Control settings remain in the existing application configuration.

The extension targets GNOME Shell 50 and is intended to be installed for the
current user.
